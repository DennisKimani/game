<ul>
<li>
  <strong>{{game.name}}:- </strong> {{game.about}}
  {{#if isImageShowing }}
  <p><img src={{game.image}} alt={{game.name}} {{action 'imageHide'}}></p>
  {{else}}
  <button {{action 'imageShow'}}> Show image</button>
  {{/if}}
</li>
</ul>
